<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.choe.mysql.dao.SubOrderMapper">


    <insert id="add" parameterType="SubOrderBean" useGeneratedKeys="true" keyProperty="id">

        insert into order_extend
        (
        orderNo,
        category_type_id,
        weight,
        unit_price,
        total_price,
        create_time
        )
        values
        (
       #{orderNo},
    #{category_type_id},
    #{weight},
    #{unit_price},
    #{total_price},
    #{create_time}
            )
    </insert>
</mapper>